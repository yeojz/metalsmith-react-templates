machine:
  node:
    version: 5.1.0
test:
  override:
    - nvm use 4.2.2 && npm run test
    - nvm use 5.1 && npm run test
deployment:
  production:
    branch: master
    commands:
      - npm install -g babel-cli
      - ./npm.sh
      - npm run build
      - npm publish
      - npm logout
